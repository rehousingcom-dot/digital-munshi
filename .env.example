# Copy this file to ".env" and fill values. NEVER commit the real .env.
SECRET_KEY=change-this-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Local test: SQLite (default, no setup needed) -> leave DATABASE_URL empty
# Server: PostgreSQL -> uncomment and set
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/erp_munshi

# Razorpay (SaaS billing). Khaali chhodo to DEV MODE chalega (bina real payment ke test).
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
